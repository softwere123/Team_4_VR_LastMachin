using System;
using UnityEngine;

public class RadialSeletion : MonoBehaviour
{
    [Range(2, 10)] // 인스팩터 창에서 조절하는 인트에 값을 스크롤 방식으로 조절할 수 있게 해줌
    public int numberofRaidialPart;
    public GameObject radialPartPrefab;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
